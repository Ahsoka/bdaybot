click
pandas
selenium
python-dotenv
python-Levenshtein
discord.py>=1.5.0
# Have to use beta version
# since it's the only version
# with asyncio support
SQLAlchemy==1.4.0b1
# If installing psycopg2
# doesn't work try installing
# psycopg2-binary instead
psycopg2
fpdf
